import React from 'react';
import Box from '@mui/material/Box';  // Asumiendo que estés usando MUI (Material-UI) para el componente Box.
type Props = {};

const PatientPage: React.FC<Props> = (props) => {
  return (
    <div>
      PatientPage
      <Box>
      </Box>
    </div>
  );
};

export default PatientPage;